# coding=utf-8

"""Main loop for tracking visualisation"""
#from sksurgerytrackervisualisation.shapes import cone, cylinder
from sksurgeryutils.common_overlay_apps import OverlayBaseApp
from sksurgerycore.configuration import ConfigurationManager

def run(config, verbose):
    """ Run the application """

    if config:
        #read in a configuration file
        #if we're going to read in a config file we should use surgery core
        pass

    else:
        #either make up some default configuration or throw an error?
        pass


    if verbose:
        pass

    else:
        pass

